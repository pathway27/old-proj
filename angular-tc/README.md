# angular-twitterclient

Reads a json file of tweets as a get request and displays it.

Live: http://pathway27.github.io/angular-tc/
--------------------------------------------

Update gh-pages in master
`git subtree push --prefix dist origin gh-pages`
